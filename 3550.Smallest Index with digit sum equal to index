class Solution(object):
    def smallestIndex(self, nums):
        for i in range(0,len(nums)):
            total=0
            for j in str(nums[i]):
                total+=int(j)
            if total==i:
                return i
        return -1
        
        
